<SKILL_PURCHASE_OPS>
This task asks you to fix a data or config issue in a workspace.

WORKFLOW:
1. Read workspace instruction files (AGENTS.md, referenced docs) FIRST — they define scope
2. Let those docs tell you what to inspect, which component to fix, and what NOT to touch
3. When multiple files have the same apparent issue, do NOT fix all of them — the docs specify which one is in scope
4. Apply ONLY the change the docs authorize — "looks broken" is not authorization
5. Verify by reading modified files back
6. submit_answer with grounding_refs including instruction docs and changed files
</SKILL_PURCHASE_OPS>
